<<<<<<< HEAD
<%= form_with model: @address,url: addresses_path do |f| %>
郵便番号
<%= f.text_field :post_code %>
住所
<%= f.text_field :address %>
名前
<%= f.text_field :name %>
=======
<div class="container">
  <div class="col-md-7 mb-5 mt-5">
    <h3>配送先/登録一覧</h3>
  </div>
    
    <%= form_with model: @address,url: addresses_path do |f| %>
    <div class="row">
      <div class="col-4">
        <table class="table table-borderless">
          <tr>
            <td>
              郵便番号<%= f.text_field :postcode %>
            </td>
            <td>
              住所<%= f.text_field :address %>
          </tr>
          <tr>
            <td>
              名前<%= f.text_field :name %>
            </td>
          </tr>
        </table>
      </div>
    </div>
    <%= f.submit "ログイン",class: 'btn btn-outline-success' %>
    <% end %>


<h5>会員登録がお済みでない方</h5>
<p><%= render "public/shared/links" %>から新規登録を行ってください</p>
</div>











>>>>>>> origin/develop
<%= f.submit '新規登録', class: 'btn btn-success' %>
<% end %>

<table class='table table-inverse'>
　<thead>
    <tr class="table-primary">
      <th>郵便番号</th>
      <th>住所</th>
      <th>宛名</th>
      <th></th>
    </tr>
  </thead>
  <tbody>
    <% @addresses.each do |f| %>
      <tr>
        <td><%= f.post_code %></td>
        <td><%= f.address %></td>
        <td><%= f.name %></td>
        <td>
          <%= link_to '編集する', edit_address_path(f), class: 'btn btn-success' %>
          <%= link_to '消去する', address_path(f),method: :delete, class: 'btn btn-danger' %>
        </td>
      </tr>
    <% end %>
  </tbody>
</table>

