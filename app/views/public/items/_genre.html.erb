  <table class="table align-middle">
    <thead>
      <th>ジャンル</th>
    </thead>

    <% @genres.each do |genre| %>
      <tbody>
        <tr><td><%= genre.name %></td></tr>
        <% end %>
      </tbody>
  </table>